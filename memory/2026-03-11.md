# 2026-03-11

## 工作内容
- 研究 OpenClaw 目录结构 (`~/.openclaw/`)
- 清理错误创建的 git 仓库 (`workspace/.git`, `dev-expert/.git`)
- 分析 CoffeeClaw 项目架构
- 确定 CoffeeClaw 新方向：AI-First 自动编程助手
- 创建符号链接：`/Users/jk/gits/hub/tools_ai/coffeeclaw/.openclaw-home` -> `~/.openclaw/`

## 详细过程
1. 发现 `.openclaw/` 目录包含完整的 OpenClaw 配置和记忆系统
2. 分析了 OpenClaw 的三层记忆结构：
   - JSONL 会话记录 (`agents/*/sessions/*.jsonl`)
   - 每日记忆 (`.openclaw/memory/YYYY-MM-DD.md`)
   - 长期记忆 (`.openclaw/MEMORY.md`)
3. 研究了 OpenClaw 的扩展机制和 coding-agent skill
4. 确定了 CoffeeClaw 的安全策略：遵循 Apple 安全约定

## 关键决策
1. **开发目录**：在手工版本 `/Users/jk/gits/hub/tools_ai/coffeeclaw/` 继续开发
2. **配置源**：直接读取 `~/.openclaw/openclaw.json`，不重复存储
3. **安全策略**：利用 macOS App Sandbox 和 TCC，不自己实现权限系统
4. **功能定位**：去掉传统代码编辑器功能，专注 AI 自动编程

## 遇到的问题
- 原始目录的 `.secrete/` 包含敏感数据，不适合复制
- OpenClaw 的 workspace 目录可能被清理，不适合存储 CoffeeClaw 数据

## 解决方案
- 创建符号链接 `.openclaw-home` 方便访问 OpenClaw 目录
- CoffeeClaw 数据存储在独立目录（如 `~/CoffeeClaw/` 或 App Support）

## 待解决问题
- 如何在切换工作目录后恢复对话记忆
- CoffeeClaw 如何写入 OpenClaw 的记忆系统

---

## Trae AI Review - 项目评审 (2026-03-11)

### 项目概况
CoffeeClaw 是一个基于 Electron + CoffeeScript 的桌面 AI 助手，集成 OpenClaw 和多个 AI 提供商。

### 当前状态
- **分支**: `refactoring`
- **主要修复已完成**: Bot 设置模态框、配置警告、OpenRouter 积分问题已文档化

### 架构亮点
1. ✅ OOP 结构清晰 (Model/Bot/Session 类)
2. ✅ TypedStorage 统一存储层
3. ✅ 提供商策略明确 (GLM-4-Flash 聊天, OpenRouter 代理)

### 改进建议
1. **测试缺失** - 建议添加 Jest/Mocha 测试框架
2. **main.coffee 过大** - 1400+ 行，建议拆分模块
3. **编译产物残留** - `src/*.js` 应从 git 中移除
4. **.openclaw/ 文档未填充** - PROJECT.md 和 TODO.md 是模板

### 待讨论问题
- OpenRouter 免费额度已用尽，需要决定解决方案
- 是否考虑从 CoffeeScript 迁移到 TypeScript？

---

*等待你的回复，可以在这里继续讨论*

---

## Trae AI 回复 (2026-03-11)

### 关于语言迁移
收到，同意暂不迁移 TypeScript。Civet 确实是个有趣的选择：
- 兼容 CoffeeScript 语法
- 输出干净的 TypeScript/JavaScript
- 但目前还在早期阶段，生态不成熟

**结论**: 保持 CoffeeScript，等 Civet 成熟后再评估。

### 下一步行动
那我们可以先聚焦其他改进：
1. **拆分 main.coffee** - 这个可以现在做
2. **添加测试框架** - 提高代码质量
3. **清理编译产物** - `git rm --cached src/*.js`

### OpenRouter 问题
需要决定：
- 升级 OpenRouter 账户？
- 等待每日额度重置？
- 创建新账户？

请告诉我你的选择，我可以帮你实施。
